feat: design system + bug fixes

Open Design system: card-base, input-base, btn-primary, btn-secondary,
focus-ring utility classes, animation suite (fadeIn, fadeInUp, scaleIn,
slideDown, pulse), skeleton shimmer, custom scrollbar.
Card component (hover/elevated variants).
Skeleton components (Skeleton, CardSkeleton, ParamSkeleton).
Header: sticky backdrop-blur, mobile hamburger menu, Modeles link.
Select: focus-visible ring, scale-in dropdown, chevron animation.
Landing: btn-primary/btn-secondary CTAs, Card sections, staggered animations.
Unified playground: input-base, btn-primary, card-base messages,
custom-scrollbar, slide-down animation on messages.
Models listing: Card hover, staggered fade-in-up grid.
Per-model pages: wrapped in animate-fade-in.
Footer: X/Twitter + GitHub social links.

Bug fixes:
- Native select in waitlist-form -> custom Select component
- Per-generation pricing instead of per-thousand (formatPriceShort -> formatLocalPrice)
- Price reactivity to parameter changes (dynamicCost useMemo accounts for
  pricePerImg * n, pricePerSec * duration, inputPrice with 1K+500 token estimate)
